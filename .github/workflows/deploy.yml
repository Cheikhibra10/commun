name: 📦 Build & Deploy commun

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      VERSION_BASE: 0.0.1

    steps:
      - name: 🧾 Checkout
        uses: actions/checkout@v4

      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: ✅ Rendre mvnw exécutable
        run: chmod +x mvnw

      - name: 🏷️ Définir la version
        id: version
        run: echo "revision=$(date +%Y%m%d%H%M%S)" >> $GITHUB_OUTPUT

      - name: ⚙️ Générer `settings.xml`
        run: |
          mkdir -p ~/.m2
          cat > ~/.m2/settings.xml <<EOF
          <settings>
            <servers>
              <server>
                <id>github</id>
                <username>${{ github.actor }}</username>
                <password>${{ secrets.GITHUB_TOKEN }}</password>
              </server>
            </servers>
          </settings>
          EOF

      - name: 📦 Build & Deploy vers GitHub Packages
        run: ./mvnw deploy -Drevision=${{ steps.version.outputs.revision }} -DskipTests

      - name: 🏷️ Créer un tag Git
        run: |
          git config user.name github-actions
          git config user.email actions@github.com
          git tag v${{ env.VERSION_BASE }}-${{ steps.version.outputs.revision }}
          git push origin v${{ env.VERSION_BASE }}-${{ steps.version.outputs.revision }}
